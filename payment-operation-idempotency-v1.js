// Préstamo Ya — idempotencia lógica de pagos v1
(()=>{'use strict';if(window.__prestamoYaPaymentOperationIdempotencyV1)return;window.__prestamoYaPaymentOperationIdempotencyV1=true;
const sid=v=>String(v??'').trim();const val=id=>document.getElementById(id)?.value??'';const normMoney=v=>Number(v||0).toFixed(2);
const keyFor=id=>[sid(id),normMoney(val('payAmount')),sid(val('payType')||'normal').toLowerCase(),sid(val('payMethod')||'').toLowerCase()].join('|');
const install=()=>{const raw=window.registerPayment;if(typeof raw!=='function')return setTimeout(install,250);if(raw.__paymentOperationIdempotencyV1)return;let activeKey='',activeAt=0;const recent=new Map();const wrapped=function(id,...args){const key=keyFor(id),now=Date.now();for(const [k,t] of recent)if(now-t>15000)recent.delete(k);if((activeKey===key&&now-activeAt<10000)||(recent.has(key)&&now-recent.get(key)<10000)){window.toast?.('Pago ya procesado. Evitando registro duplicado.');return false}activeKey=key;activeAt=now;const before=new Set((window.db?.payments||[]).map(p=>sid(p?.id)));try{const out=raw.call(this,id,...args);setTimeout(()=>{const added=(window.db?.payments||[]).find(p=>p?.id!=null&&!before.has(sid(p.id)));if(added){added.operationKey=added.operationKey||key;recent.set(key,Date.now());try{window.persist?.()}catch(_){}}activeKey='';activeAt=0},0);return out}catch(e){activeKey='';activeAt=0;throw e}};wrapped.__paymentOperationIdempotencyV1=true;window.registerPayment=wrapped};install();setTimeout(install,600);setTimeout(install,1800);window.PrestamoYaPaymentOperationIdempotency={version:'v1',keyFor};})();