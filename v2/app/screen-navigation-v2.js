// Mi Cartera PRO V2 — consistent close/back controls for every secondary screen.
(function(root){'use strict';const HOME='home';function go(target){if(typeof root.show==='function')root.show(target||HOME)}function close(){const active=document.querySelector('.page.active')?.id||'';const parent={clientForm:'clients',creditForm:'credits',agenda:'home',collections:'home',cash:'more',reports:'more',security:'more',admin:'more',more:'home',clients:'home',credits:'home'}[active]||HOME;go(parent)}function install(){document.querySelectorAll('main .page').forEach(page=>{if(page.id===HOME||page.querySelector(':scope > .screen-toolbar'))return;const bar=document.createElement('div');bar.className='screen-toolbar';bar.innerHTML='<button type="button" class="btn screen-close" aria-label="Cerrar pantalla">✕ Cerrar</button><button type="button" class="btn screen-home" aria-label="Ir al inicio">⌂ Inicio</button>';bar.querySelector('.screen-close').addEventListener('click',close);bar.querySelector('.screen-home').addEventListener('click',()=>go(HOME));page.prepend(bar)})}document.addEventListener('DOMContentLoaded',install);root.MiCarteraV2ScreenNavigation={install,close,go};})(window);
