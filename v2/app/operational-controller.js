// Mi Cartera PRO V2 — operational UI controller. Validation storage is V2-only.
(function(root){'use strict';const K='mi-cartera-v2-validation-state';const F=root.MiCarteraV2Financial,S=root.MiCarteraV2Schedule;if(!F||!S)return;const money=F.money;let state={clients:[],credits:[],payments:[]};try{state={...state,...JSON.parse(localStorage.getItem(K)||'{}')}}catch{}const save=()=>localStorage.setItem(K,JSON.stringify(state));const id=p=>p+'-'+Date.now()+'-'+Math.random().toString(36).slice(2,7);const byId=x=>document.getElementById(x);function render(){if(byId('mClients'))byId('mClients').textContent=state.clients.length;if(byId('mPortfolio'))byId('mPortfolio').textContent='S/'+money(state.credits.filter(c=>c.status==='ACTIVO').reduce((n,c)=>n+F.principalBalance(c),0)).toFixed(2);if(byId('mCollected'))byId('mCollected').textContent='S/'+money(state.payments.reduce((n,p)=>n+p.amount,0)).toFixed(2);if(byId('clientsList'))byId('clientsList').innerHTML=state.clients.length?state.clients.map(c=>`<div><b>${c.name}</b><br>${c.dni||''} ${c.phone||''}</div>`).join('<hr>'):'Sin datos cargados en V2.';if(byId('creditsList'))byId('creditsList').innerHTML=state.credits.length?state.credits.map(c=>`<div><b>${c.id}</b> · ${c.status}<br>Capital S/${c.capital.toFixed(2)} · Saldo S/${F.principalBalance(c).toFixed(2)}<br><button class="btn green" onclick="V2UI.collect('${c.id}')">Cobrar</button></div>`).join('<hr>'):'Sin créditos cargados en V2.'}
function addClient(){const name=byId('fName').value.trim(),phone=byId('fPhone').value.trim();if(!name||!phone)return alert('Nombre y teléfono son obligatorios.');state.clients.push({id:id('cli'),name,phone,dni:byId('fDni').value.trim(),address:byId('fAddress').value.trim(),route:byId('fRoute').value.trim(),version:1});save();render();show('clients')}
function addCredit(){const clientId=byId('cClient').value.trim(),client=state.clients.find(c=>c.id===clientId)||state.clients.find(c=>c.name.toLowerCase()===clientId.toLowerCase());if(!client)return alert('Cliente no encontrado. Usa su ID o nombre exacto.');const capital=money(byId('cCapital').value),rate=Number(byId('cRate').value||0),term=Number(byId('cTerm').value),freq=byId('cFreq').value,first=byId('cFirst').value;if(!(capital>0)||!Number.isInteger(term)||term<1||!first)return alert('Completa capital, cuotas y primera fecha.');const total=money(capital*(1+rate/100));const schedule=S.generate({total,term,freq,firstPaymentDate:first});state.credits.push({id:id('cre'),clientId:client.id,capital,total,rate,term,freq,firstPaymentDate:first,schedule,principalPaid:0,interestPaid:0,penaltyPaid:0,status:'ACTIVO',version:1});save();render();show('credits')}
function collect(creditId){const c=state.credits.find(x=>x.id===creditId);if(!c||c.status!=='ACTIVO')return;const raw=prompt('Monto a cobrar (capital):',F.principalBalance(c).toFixed(2));if(raw===null)return;const amount=money(raw);try{const r=F.allocatePayment(c,{id:id('pay'),amount,concept:F.CONCEPT.CAPITAL,date:new Date().toISOString().slice(0,10)});Object.assign(c,r.credit);state.payments.push(r.payment);save();render()}catch(e){alert(e.message)}}
root.V2UI={addClient,addCredit,collect,render};document.addEventListener('DOMContentLoaded',()=>{const sc=byId('saveClient'),scr=byId('saveCredit');if(sc)sc.onclick=addClient;if(scr)scr.onclick=addCredit;render()});})(window);