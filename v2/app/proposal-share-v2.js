// Mi Cartera PRO V2 — credit proposal sharing. This module NEVER creates or mutates a credit.
(function(root){'use strict';const money=n=>'S/ '+Number(n||0).toLocaleString('es-PE',{minimumFractionDigits:2,maximumFractionDigits:2});const frequencyLabel=f=>({daily:'Diaria',weekly:'Semanal',biweekly:'Quincenal',monthly:'Mensual'}[f]||f);function draft(){const $=id=>document.getElementById(id),q=$('cClient')?.value.trim()||'',capital=Number($('cCapital')?.value||0),rate=Number($('cRate')?.value||0),term=Number($('cTerm')?.value||0),freq=$('cFreq')?.value||'',first=$('cFirst')?.value||'';if(!q||!(capital>0)||!Number.isInteger(term)||term<1||!first)throw new Error('Completa cliente, capital, cuotas y primera fecha antes de compartir.');const total=Math.round(capital*(1+rate/100)*100)/100,installment=Math.round(total/term*100)/100,maturity=root.MiCarteraV2CreditFormParity?.maturity?.(first,term,freq)||'';return {client:q,capital,rate,term,freq,first,maturity,total,installment}}function message(d){return `MI CARTERA PRO — PROPUESTA DE CRÉDITO\nCliente: ${d.client}\nCapital: ${money(d.capital)}\nInterés: ${d.rate}%\nTotal a pagar: ${money(d.total)}\nCuotas: ${d.term}\nCuota referencial: ${money(d.installment)}\nFrecuencia: ${frequencyLabel(d.freq)}\nPrimera fecha de pago: ${d.first}${d.maturity?`\nFecha estimada de última cuota: ${d.maturity}`:''}\n\nEsta es una propuesta. El crédito NO queda registrado hasta su aceptación y posterior guardado en Mi Cartera PRO.`}function findClient(d){const K='mi-cartera-v2-validation-state';let db={};try{db=JSON.parse(localStorage.getItem(K)||'{}')}catch{}return (db.clients||[]).find(x=>String(x.id)===d.client)||(db.clients||[]).find(x=>String(x.name||'').toLowerCase()===d.client.toLowerCase())}async function share(){const d=draft(),text=message(d),client=findClient(d);if(navigator.share){try{await navigator.share({title:'Propuesta de crédito · Mi Cartera PRO',text});return true}catch(e){if(e?.name==='AbortError')return false}}if(client?.phone&&root.MiCarteraV2CustomerExperience?.whatsapp){root.MiCarteraV2CustomerExperience.whatsapp(client,text);return true}if(navigator.clipboard?.writeText){await navigator.clipboard.writeText(text);alert(client?'Propuesta copiada. El crédito todavía NO fue creado.':'Propuesta copiada. El cliente aún no está registrado en V2; puedes pegarla en WhatsApp u otra aplicación.');return true}throw Object.assign(new Error('No se pudo compartir la propuesta. Registra el teléfono del cliente o usa un dispositivo compatible con compartir/copiar.'),{code:'SHARE_NOT_SUPPORTED'})}root.MiCarteraV2ProposalShare={draft,message,findClient,share};})(window);
